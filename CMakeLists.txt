cmake_minimum_required(VERSION 2.8)
SET(CMAKE_MODULE_PATH "$ENV{OSSIM_DEV_HOME}/osg/CMakeModules;${CMAKE_MODULE_PATH}")
include_directories( "$ENV{OSG_ROOT_DIR}/include" )
SET(OpenThreads_SOURCE_DIR "$ENV{OSG_ROOT_DIR}")
SET(PROJECT_SOURCE_DIR "$ENV{OSG_ROOT_DIR}")
subdirs($ENV{OSG_ROOT_DIR}/src/OpenThreads)
